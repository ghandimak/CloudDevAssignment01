function printStudents(students) {
    for (let i = 0; i < students.length; i++) {
      console.log(students[i]);
    }
  }
  
  const students = ["John", "Kate", "Amy", "Tom"];
  printStudents(students);